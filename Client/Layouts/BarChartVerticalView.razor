<table style="height:fit-content;z-index:10;border:none;margin-top: auto;">
    <tbody>
        @foreach (var chartBar in ChartBars)
        {
            <tr>
                <td style="float:right;padding-right:0.5rem">
                    @chartBar.Name
                </td>
                <td style="vertical-align:top">
                    <div style="width:10rem;height:1rem">
                        <div class="v_bar" style=@($"--bar-value:{chartBar.CalculatePer}%;--bar-color-value:{chartBar.CalculatePer}")>
                            @chartBar.Text
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

@code {
    [Parameter]
    public List<ChartBar> ChartBars { get; set; } = new();
}

<style>
    .v_bar {
        width: var(--bar-value);
        height: 100%;
        background-image: linear-gradient(to right, hsl(var(--bar-color-value),50%,57%),hsl(var(--bar-color-value),50%,50%),hsl(var(--bar-color-value),50%,57%) );
        border-radius: 2px;
        box-shadow: 8px 4px 8px hsl(0,0%,70%);
        position: relative;
        z-index: 100;
    }
</style>