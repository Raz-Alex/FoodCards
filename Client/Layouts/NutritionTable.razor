@using FoodCards.Client.Script

@if (Nutrients != null)
{
	<table>
		<tbody>
			<tr>
				<th>
					@DictionaryService.CalculateFor
				</th>
				<td>
					100g
				</td>
			</tr>
			<tr>
				<th>
					@DictionaryService.Kalories
				</th>
				<td>
					@($"{Kalories}kcal")
				</td>
			</tr>
			@foreach (var item in Nutrients)
			{
				@if (item.Value > 0)
				{
					<tr>
						<th>
							@item.Key
						</th>
						<td>
							@($"{item.Value}g")
						</td>
					</tr>
				}
			}
		</tbody>
	</table>
}

@code {
	[Parameter]
	public decimal Kalories{ get; set; }

	[Parameter]
	public Dictionary<string, decimal> Nutrients { get; set; }
}
